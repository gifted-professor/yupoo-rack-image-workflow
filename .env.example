# The tested bridge route is:
# this project -> Amazon image bridge :8907 -> CPA :8317 -> gpt-image-2
IMAGE_BRIDGE_URL=http://127.0.0.1:8907/api/image/generate

# Feishu status table (飞书多维表格). Read by sync-catalog.mjs, written by push-catalog-status.mjs.
# Requires the `lark-cli` npm package installed globally and logged in (`lark-cli login`).
# The defaults below are the shared status board; override only if you target a different table.
FEISHU_BASE_TOKEN=PYtZbqyPyafc4sscwdjcQNLNnEh
FEISHU_TABLE_ID=tblUcslarq5iLEPB
# Review dashboard base URL, used in the 备注 column link.
REVIEW_URL_BASE=http://127.0.0.1:8910/
